apiVersion: v1
kind: Service
metadata:
  name: {{ include "tic-tac-toe-helm.fullname" . }}
  labels:
    {{- include "tic-tac-toe-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tic-tac-toe-helm.selectorLabels" . | nindent 4 }}
