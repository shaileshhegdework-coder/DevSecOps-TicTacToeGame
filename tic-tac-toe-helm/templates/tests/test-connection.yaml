apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tic-tac-toe-helm.fullname" . }}-test-connection"
  labels:
    {{- include "tic-tac-toe-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tic-tac-toe-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
